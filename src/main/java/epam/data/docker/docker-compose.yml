version: '3.1'
services:
  db:
    image: postgres:10
    restart: always
    environment:
      POSTGRES_DB: data
      POSTGRES_USER: data
      POSTGRES_PASSWORD: data
    ports:
      - "5436:5436"